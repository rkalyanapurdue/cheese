kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: access-arpspoof-public
spec:
  podSelector:
    matchLabels:
      group: rkalyana-public
  ingress:
  - ports:
    - port: 8888
  - from:
    - podSelector:
        matchExpressions:
              - {key: group, operator: DoesNotExist, values: []}

